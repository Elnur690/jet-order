export default {
  plugins: {
    // ⛔️ REMOVE the old way:
    // tailwindcss: {},

    // ✅ ADD the new way, using the new package name:
    '@tailwindcss/postcss': {},

    autoprefixer: {},
  },
};